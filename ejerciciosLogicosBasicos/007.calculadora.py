import tkinter as tk # Importamos la libreria de tkinter
app = tk.Tk() # Con el app declaramos una ventana
app.title("Mi Calculadora") # Le ponemos un titulo
app.geometry("400x300") # Asignamos las dimensiones de la pantalla
app.mainloop() # Mantenemos la pantalla de loop
